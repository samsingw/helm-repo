apiVersion: v1
kind: Service
metadata:
  name: {{ include "openhab4.fullname" . }}
  labels:
    {{- include "openhab4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openhab4.selectorLabels" . | nindent 4 }}
