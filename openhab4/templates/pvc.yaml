apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openhab-data-pvc
  namespace: openhab
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
